{% extends 'base.html.twig' %}



{% block title %}Tag index
{% endblock %}

{% block content %}
    <h1>Tags</h1>
    <div class='container-table'>
        <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tag.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ tag.name }}</td>
            </tr>
        </tbody>
    </table>
    </div>
    <a href="{{ path('backend_tag_index') }}">Retour à la liste</a>

    <a href="{{ path('backend_tag_edit', {'id':tag.id}) }}">Editer</a>
        {{ include('backend/tag/_delete_form.html.twig') }}


{% endblock %}
